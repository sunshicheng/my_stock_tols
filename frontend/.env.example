# 后端 API 根地址（仅打包安卓 App 时需要配置）
# - 网页版 / 开发：前端与后端同源时留空即可，请求会走相对路径
# - 打包安卓：App 内请求必须写死服务器地址，在打 App 包时设置，例如：
#   VITE_API_BASE=https://your-server.com npm run build
#   然后 npx cap sync。不要用结尾斜杠。
# VITE_API_BASE=
